const STORE = {
  nameJa: "カヌン タイ リラクゼーション 南海難波店",
  nameEn: "Kanun Thai Relaxation Nankai Namba",
  phone: "+81644009889",
  phoneDisplay: "06-4400-9889",
  whatsapp: "85290744644",
  maps: "https://www.google.com/maps/search/?api=1&query=%E3%80%92556-0011+%E5%A4%A7%E9%98%AA%E5%B8%82%E6%B5%AA%E9%80%9F%E5%8C%BA%E9%9B%A3%E6%B3%A2%E4%B8%AD1%E4%B8%81%E7%9B%AE18-8+K2%E3%83%93%E3%83%AB+2F",
  mapEmbed: "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3281.636797420285!2d135.50034300000002!3d34.6638744!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x6000e7132607418b%3A0x9190e1d2a9033e64!2z44Kr44OM44OzIOOCv-OCpCDjg6rjg6njgq_jgrzjg7zjgrfjg6fjg7Mg5Y2X5rW36Zuj5rOi5bqX!5e0!3m2!1szh-CN!2s!4v1778482649762!5m2!1szh-CN!2s",
  addressJa: "〒556-0011 大阪府大阪市浪速区難波中1丁目18-8 K2ビル2階",
  addressEn: "〒556-0011 Osaka, Naniwa Ward, Nanbanaka, 1 Chome−18−8 K2ビル 2F",
  station: "Namba Station"
};

const COPY = {
  en: {
    code: "EN",
    title: "Kanun Thai Relaxation Nankai Namba",
    kicker: "Thai relaxation salon in Osaka Namba",
    lead: "",
    priceListLabel: "Price List",
    navBooking: "WhatsApp booking",
    phone: STORE.phoneDisplay,
    maps: "Google Maps",
    galleryTitle: "Salon gallery",
    introTitle: "Easy access in Namba",
    intro: "Body care, aroma treatment, foot reflexology, and head massage for travel fatigue and work tension in Namba, Osaka",
    info: ["Hours", "Daily 11:00–00:00", "Phone", STORE.phoneDisplay, "Phone booking available", "Address", STORE.addressEn, "About 1–2 min walk from Nankai Namba Station West Exit"],
    servicesTitle: "Services",
    servicesLead: "Choose a short refresh or a longer treatment depending on your schedule.",
    services: [
      ["Body massage", "Classic full-body care for shoulder, back, and leg fatigue."],
      ["Aroma massage", "Relaxing oil treatment in a calm private-room setting."],
      ["Foot reflexology", "Focused foot care for tired legs after walking around Osaka."],
      ["Head massage", "Scalp and neck relaxation for screen fatigue and heavy heads."]
    ],
    menuTitle: "Menu & Prices",
    menuLead: "All prices include tax.",
    singleMenuTitle: "Single Treatments",
    comboMenuTitle: "Combination Courses",
    durationLabel: "Duration",
    priceLabel: "Price",
    popular: "Popular",
    recommended: "Recommended",
    extension: "Extension",
    note: "Notes",
    oilNote: "Thai and oil (60 min or more) include a foot bath. Foot reflexology includes a foot bath. Ask us about extensions.",
    nominateNote: "Staff nomination fee: ¥300.",
    visitTitle: "Visit",
    visitLead: "",
    visitWeekLines: [
      "Monday: 11:00 – 00:00",
      "Tuesday: 11:00 – 00:00",
      "Wednesday: 11:00 – 00:00",
      "Thursday: 11:00 – 00:00",
      "Friday: 11:00 – 00:00",
      "Saturday: 11:00 – 00:00",
      "Sunday: 11:00 – 00:00"
    ],
    footer: "Kanun Thai Relaxation Nankai Namba"
  },
  jp: {
    code: "JP",
    title: "カヌン タイ リラクゼーション 南海難波店",
    kicker: "難波のタイ式リラクゼーション",
    lead: "",
    priceListLabel: "価格表",
    navBooking: "WhatsApp 予約",
    phone: STORE.phoneDisplay,
    maps: "Google Maps",
    galleryTitle: "店内ギャラリー",
    introTitle: "南海難波駅すぐそば",
    intro: "全身もみほぐし、アロマ、足つぼ、ヘッドマッサージなど、旅行中やお仕事帰りの疲れに合わせて選べます",
    info: ["営業時間", "毎日 11:00〜00:00", "電話", STORE.phoneDisplay, "電話予約可", "住所", STORE.addressJa, "南海なんば駅西出口から徒歩約1–2分"],
    servicesTitle: "メニュー",
    servicesLead: "短時間のリフレッシュから、ゆっくり受けたいロングコースまで。",
    services: [
      ["全身もみほぐし", "肩、背中、脚など日常の疲れを丁寧にケア。"],
      ["アロママッサージ", "落ち着いた空間で受けるリラックスオイルトリートメント。"],
      ["足つぼ", "大阪観光で歩き疲れた足を集中的にケア。"],
      ["ヘッドマッサージ", "首まわりや目の疲れ、頭の重さをやさしくリセット。"]
    ],
    menuTitle: "価格表",
    menuLead: "税込。",
    singleMenuTitle: "単品メニュー",
    comboMenuTitle: "組み合わせコース",
    durationLabel: "時間",
    priceLabel: "料金",
    popular: "人気",
    recommended: "おすすめ",
    extension: "延長",
    note: "備考",
    oilNote: "タイ古式・オイルは60分以上で足湯付き。足ツボは足湯付き。延長をご希望の場合はお申し付けください。",
    nominateNote: "指名料：¥300。",
    visitTitle: "アクセス",
    visitLead: "",
    visitWeekLines: [
      "月曜：11:00〜00:00",
      "火曜：11:00〜00:00",
      "水曜：11:00〜00:00",
      "木曜：11:00〜00:00",
      "金曜：11:00〜00:00",
      "土曜：11:00〜00:00",
      "日曜：11:00〜00:00"
    ],
    footer: "カヌン タイ リラクゼーション 南海難波店"
  },
  cn: {
    code: "CN",
    title: "Kanun 泰式放松 南海难波店",
    kicker: "大阪难波泰式放松沙龙",
    lead: "",
    priceListLabel: "价格表",
    navBooking: "WhatsApp 预约",
    phone: STORE.phoneDisplay,
    maps: "Google Maps",
    galleryTitle: "环境展示",
    introTitle: "难波站附近，方便到店",
    intro: "提供全身按摩、精油芳疗、足底护理、头部按摩等项目，适合旅行后放松和工作后舒缓",
    info: ["营业时间", "每天 11:00–00:00", "电话", STORE.phoneDisplay, "可电话预约", "地址", "〒556-0011 大阪市浪速区难波中1丁目18-8 K2大楼 2楼", "从南海难波站西出口步行约1–2分钟"],
    servicesTitle: "服务项目",
    servicesLead: "可按行程选择短时间放松或较长护理。",
    services: [
      ["全身按摩", "针对肩颈、背部和腿部疲劳的经典全身护理。"],
      ["精油按摩", "在安静空间中享受舒缓放松的芳香护理。"],
      ["足底护理", "适合大阪旅行步行后的腿足疲劳。"],
      ["头部按摩", "舒缓用眼疲劳、头重和颈部紧张。"]
    ],
    menuTitle: "项目与价格",
    menuLead: "以下价格均含税。",
    singleMenuTitle: "单项项目",
    comboMenuTitle: "组合套餐",
    durationLabel: "时长",
    priceLabel: "价格",
    popular: "人气",
    recommended: "推荐",
    extension: "延长",
    note: "备注",
    oilNote: "泰式古法与精油60分钟及以上含足浴。足底按摩含足浴。如需延长时间请告知。",
    nominateNote: "指定工作人员费用：¥300。",
    visitTitle: "位置",
    visitLead: "",
    visitWeekLines: [
      "星期一：11:00 – 00:00",
      "星期二：11:00 – 00:00",
      "星期三：11:00 – 00:00",
      "星期四：11:00 – 00:00",
      "星期五：11:00 – 00:00",
      "星期六：11:00 – 00:00",
      "星期日：11:00 – 00:00"
    ],
    footer: "Kanun 泰式放松 南海难波店"
  },
  tw: {
    code: "TW",
    title: "Kanun 泰式放鬆 南海難波店",
    kicker: "大阪難波泰式放鬆沙龍",
    lead: "",
    priceListLabel: "價格表",
    navBooking: "WhatsApp 預約",
    phone: STORE.phoneDisplay,
    maps: "Google Maps",
    galleryTitle: "環境展示",
    introTitle: "難波站附近，方便到店",
    intro: "提供全身按摩、精油芳療、足底護理、頭部按摩等項目，適合旅行後放鬆與工作後舒緩",
    info: ["營業時間", "每天 11:00–00:00", "電話", STORE.phoneDisplay, "可電話預約", "地址", "〒556-0011 大阪市浪速區難波中1丁目18-8 K2大樓 2樓", "從南海難波站西出口步行約1–2分鐘"],
    servicesTitle: "服務項目",
    servicesLead: "可依行程選擇短時間放鬆或較長護理。",
    services: [
      ["全身按摩", "針對肩頸、背部與腿部疲勞的經典全身護理。"],
      ["精油按摩", "在安靜空間中享受舒緩放鬆的芳香護理。"],
      ["足底護理", "適合大阪旅行步行後的腿足疲勞。"],
      ["頭部按摩", "舒緩用眼疲勞、頭重與頸部緊繃。"]
    ],
    menuTitle: "項目與價格",
    menuLead: "以下價格均含稅。",
    singleMenuTitle: "單項項目",
    comboMenuTitle: "組合套餐",
    durationLabel: "時長",
    priceLabel: "價格",
    popular: "人氣",
    recommended: "推薦",
    extension: "延長",
    note: "備註",
    oilNote: "泰式古法與精油60分鐘及以上含足浴。足底按摩含足浴。如需延長時間請告知。",
    nominateNote: "指定工作人員費用：¥300。",
    visitTitle: "位置",
    visitLead: "",
    visitWeekLines: [
      "星期一：11:00 – 00:00",
      "星期二：11:00 – 00:00",
      "星期三：11:00 – 00:00",
      "星期四：11:00 – 00:00",
      "星期五：11:00 – 00:00",
      "星期六：11:00 – 00:00",
      "星期日：11:00 – 00:00"
    ],
    footer: "Kanun 泰式放鬆 南海難波店"
  },
  kr: {
    code: "KR",
    title: "카눈 타이 릴랙세이션 난카이 난바점",
    kicker: "오사카 난바 타이 릴랙세이션",
    lead: "",
    priceListLabel: "가격표",
    navBooking: "WhatsApp 예약",
    phone: STORE.phoneDisplay,
    maps: "Google Maps",
    galleryTitle: "매장 분위기",
    introTitle: "난바역 근처 마사지 살롱",
    intro: "전신 마사지, 아로마 트리트먼트, 발 케어, 헤드 마사지 등 여행 피로와 업무 후 긴장을 풀기 좋은 메뉴를 제공합니다",
    info: ["영업시간", "매일 11:00–00:00", "전화", STORE.phoneDisplay, "전화 예약 가능", "주소", "〒556-0011 오사카시 나니와구 난바나카 1초메 18-8 K2빌딩 2층", "난카이 난바역 서쪽 출구에서 도보 약 1–2분"],
    servicesTitle: "서비스",
    servicesLead: "짧은 휴식부터 여유로운 코스까지 일정에 맞게 선택할 수 있습니다.",
    services: [
      ["전신 마사지", "어깨, 등, 다리 피로를 위한 기본 전신 케어."],
      ["아로마 마사지", "차분한 공간에서 받는 릴랙스 오일 트리트먼트."],
      ["발 케어", "오사카 관광으로 지친 발과 다리를 집중 케어."],
      ["헤드 마사지", "눈의 피로, 무거운 머리, 목 긴장을 부드럽게 완화."]
    ],
    menuTitle: "메뉴 및 가격",
    menuLead: "가격은 세금 포함입니다.",
    singleMenuTitle: "단품 메뉴",
    comboMenuTitle: "콤비네이션 코스",
    durationLabel: "시간",
    priceLabel: "가격",
    popular: "인기",
    recommended: "추천",
    extension: "연장",
    note: "비고",
    oilNote: "타이·오일 60분 이상 코스는 족욕 포함. 발 지압은 족욕 포함. 연장을 원하시면 말씀해 주세요.",
    nominateNote: "직원 지명료: ¥300.",
    visitTitle: "오시는 길",
    visitLead: "",
    visitWeekLines: [
      "월요일: 11:00 – 00:00",
      "화요일: 11:00 – 00:00",
      "수요일: 11:00 – 00:00",
      "목요일: 11:00 – 00:00",
      "금요일: 11:00 – 00:00",
      "토요일: 11:00 – 00:00",
      "일요일: 11:00 – 00:00"
    ],
    footer: "카눈 타이 릴랙세이션 난카이 난바점"
  }
};

const COMBO_PAGE_LABELS = {
  en: { viewAll: "View all combination courses", back: "Back" },
  jp: { viewAll: "組み合わせコースをすべて見る", back: "戻る" },
  cn: { viewAll: "查看全部组合套餐", back: "返回" },
  tw: { viewAll: "查看全部組合套餐", back: "返回" },
  kr: { viewAll: "전체 조합 코스 보기", back: "뒤로" }
};

const LANGS = [
  ["en", "EN"],
  ["jp", "JP"],
  ["cn", "简"],
  ["tw", "繁"],
  ["kr", "KR"]
];

const BOOKING_LABELS = {
  en: "Book",
  jp: "予約",
  cn: "预约",
  tw: "預約",
  kr: "예약"
};

const MORE_LABELS = {
  en: "More",
  jp: "もっと見る",
  cn: "更多",
  tw: "更多",
  kr: "더보기"
};

function badge(type, copy) {
  if (!type) return "";
  const label = type === "popular" ? copy.popular : type === "recommended" ? copy.recommended : copy.extension;
  return `<span class="menu-badge menu-badge--${type}">${label}</span>`;
}

function shopBookingHref(query) {
  const qs = query instanceof URLSearchParams ? query.toString() : String(query || "");
  const suffix = qs ? `booking.html?${qs}` : "booking.html";
  const slug = (location.pathname.match(/\/shops\/([^/]+)/) || [])[1];
  if (slug) return `/shops/${slug}/${suffix}`;
  return `../${suffix}`;
}

function generalBookingUrl(lang) {
  return shopBookingHref(new URLSearchParams({ lang }));
}

function bookingUrl(lang, service, duration, price) {
  return shopBookingHref(new URLSearchParams({ lang, service, duration, price }));
}

function bookingButton(lang, service, duration, price, type) {
  if (typeof isAddonItem === "function" && isAddonItem(duration, type)) return "";
  const label = BOOKING_LABELS[lang] || BOOKING_LABELS.en;
  return `<a class="menu-book-btn" href="${bookingUrl(lang, service, duration, price)}">${label}</a>`;
}

function isPrimaryMenuItem(duration, type) {
  return type === "popular" || type === "recommended" || /(^|\s)(60|90|120)\s*min/i.test(duration);
}

function renderSingleRow(copy, lang, service, duration, price, type) {
  return `
    <div class="menu-row">
      <span>${duration}${badge(type, copy)}</span>
      <strong>${price}</strong>
      ${bookingButton(lang, service, duration, price, type)}
    </div>
  `;
}

function renderComboRow(copy, lang, service, name, duration, price, type) {
  const localizedName = localizeMenuName(name, lang);
  return `
    <div class="combo-row">
      <span>${localizedName}${badge(type, copy)}</span>
      <em>${duration}</em>
      <strong>${price}</strong>
      ${bookingButton(lang, `${service} - ${localizedName}`, duration, price)}
    </div>
  `;
}

function renderMoreBlock(lang, rows) {
  if (!rows.length) return "";
  const label = MORE_LABELS[lang] || MORE_LABELS.en;
  return `
    <details class="menu-more">
      <summary>${label}</summary>
      <div class="menu-more-list">${rows.join("")}</div>
    </details>
  `;
}

function splitRowsByPriority(items, durationIndex, typeIndex) {
  if (items.length <= 3) {
    return { primary: items, secondary: [] };
  }

  const primary = items.filter((item) => isPrimaryMenuItem(item[durationIndex], item[typeIndex]));
  return {
    primary: primary.length ? primary : items.slice(0, 3),
    secondary: primary.length
      ? items.filter((item) => !isPrimaryMenuItem(item[durationIndex], item[typeIndex]))
      : items.slice(3)
  };
}

function renderSingleMenu(copy, lang) {
  return SINGLE_MENU.map((group) => {
    const rows = splitRowsByPriority(group.items, 0, 2);
    return `
      <article class="menu-card">
        <h3>${MENU_LABELS[group.key][lang]}</h3>
        <div class="menu-list">
          ${rows.primary
            .map(([duration, price, type]) => renderSingleRow(copy, lang, MENU_LABELS[group.key][lang], duration, price, type))
            .join("")}
          ${renderMoreBlock(lang, rows.secondary
            .map(([duration, price, type]) => renderSingleRow(copy, lang, MENU_LABELS[group.key][lang], duration, price, type)))}
        </div>
      </article>
    `;
  }).join("");
}

function renderComboMenu(copy, lang) {
  const limitGroups = arguments.length > 2 && arguments[2] && typeof arguments[2].limitGroups === "number"
    ? arguments[2].limitGroups
    : null;
  const groups = typeof limitGroups === "number" ? COMBO_MENU.slice(0, limitGroups) : COMBO_MENU;

  return groups.map((group) => {
    const rows = splitRowsByPriority(group.items, 1, 3);
    return `
      <article class="menu-card menu-card--wide">
        <h3>${MENU_LABELS[group.key][lang]}</h3>
        <div class="combo-list">
          ${rows.primary
            .map(([name, duration, price, type]) => renderComboRow(copy, lang, MENU_LABELS[group.key][lang], name, duration, price, type))
            .join("")}
          ${renderMoreBlock(lang, rows.secondary
            .map(([name, duration, price, type]) => renderComboRow(copy, lang, MENU_LABELS[group.key][lang], name, duration, price, type)))}
        </div>
      </article>
    `;
  }).join("");
}

function render() {
  const lang = document.body.dataset.lang || "en";
  const copy = COPY[lang] || COPY.en;
  document.body.classList.add("shop-page");

  const isComboPage = /combo\.html$/i.test(window.location.pathname || "");
  const comboLabels = COMBO_PAGE_LABELS[lang] || COMBO_PAGE_LABELS.en;

  const galleryImages = [
    { src: "../images/2.webp", alt: "" },
    { src: "../images/3.webp", alt: "" },
    { src: "../images/4.webp", alt: "" },
    { src: "../images/5.webp", alt: "" }
  ];

  const languageLinks = LANGS.map(([key, label]) => {
    const current = key === lang ? ' aria-current="page"' : "";
    const page = isComboPage ? "combo.html" : "index.html";
    return `<a href="../${key}/${page}"${current}>${label}</a>`;
  }).join("");

  document.getElementById("app").innerHTML = `
    <header class="topbar">
      <div class="topbar-inner">
        <a class="brand" href="../landing.html">${STORE.nameJa}</a>
        <div class="topbar-actions">
          <nav class="language-switch" aria-label="Language switcher">${languageLinks}</nav>
          <button class="lang-menu-btn" type="button" aria-label="Open language menu" aria-expanded="false" aria-controls="lang-menu">
            <span></span>
            <span></span>
            <span></span>
          </button>
        </div>
      </div>
      <div class="lang-menu" id="lang-menu" hidden>
        <nav class="lang-menu-list" aria-label="Language menu">${languageLinks}</nav>
      </div>
    </header>

    <main class="page-shell">
      ${isComboPage ? `
        <section class="section" id="combo">
          <div class="menu-panel">
            <div class="section-head">
              <a class="back-link" href="./index.html">${comboLabels.back}</a>
              <p class="eyebrow">${copy.priceListLabel || "Price List"}</p>
              <h2>${copy.comboMenuTitle}</h2>
            </div>
            <div class="menu-grid menu-grid--combo">${renderComboMenu(copy, lang)}</div>
            <ul class="sheet-note"><li>${copy.nominateNote}</li></ul>
          </div>
        </section>
      ` : `
      <section class="hero">
        <div class="hero-content">
          <p class="eyebrow">${copy.kicker}</p>
          <h1>${copy.title}</h1>
          <p class="hero-intro">${copy.intro}</p>
          ${copy.lead ? `<p class="lead">${copy.lead}</p>` : ""}
          <div class="hero-actions">
            <a class="btn btn-whatsapp" href="${generalBookingUrl(lang)}">${copy.navBooking}</a>
            <a class="btn btn-outline" href="tel:${STORE.phone}">${copy.phone}</a>
          </div>
        </div>
      </section>

      <section class="section">
        <div class="photo-slot" aria-label="Store photo">
          <img class="photo-slot__img" src="../images/2.webp" alt="" loading="lazy">
        </div>
      </section>

      <section class="section" id="menu">
        <div class="menu-panel">
          <div class="section-head">
            <p class="eyebrow">${copy.priceListLabel || "Price List"}</p>
            <h2>${copy.menuTitle}</h2>
            ${copy.menuLead ? `<p class="section-lead">${copy.menuLead}</p>` : ""}
          </div>
          <div class="menu-note">
            <span>${copy.oilNote}</span>
          </div>
          <h3 class="menu-section-title">${copy.singleMenuTitle}</h3>
          <div class="menu-grid">${renderSingleMenu(copy, lang)}</div>
          <h3 class="menu-section-title">${copy.comboMenuTitle}</h3>
          <div class="menu-grid menu-grid--combo">${renderComboMenu(copy, lang, { limitGroups: 2 })}</div>
          <div class="menu-section-actions">
            <a class="btn btn-outline combo-viewall" href="./combo.html">${comboLabels.viewAll}</a>
          </div>
          <ul class="sheet-note"><li>${copy.nominateNote}</li></ul>
        </div>
      </section>

      <section class="section" aria-label="${copy.galleryTitle}">
        <div class="section-head section-head--center">
          <h2>${copy.galleryTitle}</h2>
        </div>
        <div class="carousel" data-carousel>
          <div class="carousel-viewport">
            <div class="carousel-track">
              ${galleryImages.map((img, idx) => `
                <div class="carousel-slide" data-slide="${idx}">
                  <img src="${img.src}" alt="${img.alt}" loading="lazy">
                </div>
              `).join("")}
            </div>
          </div>
          <button class="carousel-btn carousel-btn--prev" type="button" aria-label="Previous image" data-carousel-prev>‹</button>
          <button class="carousel-btn carousel-btn--next" type="button" aria-label="Next image" data-carousel-next>›</button>
          <div class="carousel-dots" role="tablist" aria-label="Select image">
            ${galleryImages.map((_, idx) => `
              <button class="carousel-dot" type="button" aria-label="Go to image ${idx + 1}" data-carousel-dot="${idx}"></button>
            `).join("")}
          </div>
        </div>
      </section>

      <section class="section">
        <div class="map-section">
          <div class="map-copy">
            <p class="eyebrow">Access</p>
            <h2>${copy.visitTitle}</h2>
            <p class="address">${copy.info[6]}</p>
            <div class="visit-facts">
              <p class="visit-facts__near">${copy.info[7]}</p>
              <p class="visit-week__summary"><strong>${copy.info[0]}:</strong> ${copy.info[1]}</p>
            </div>
            ${copy.visitLead ? `<p>${copy.visitLead}</p>` : ""}
            <div class="nav-actions">
              <a class="btn btn-whatsapp" href="${generalBookingUrl(lang)}">${copy.navBooking}</a>
              <a class="btn btn-dark" href="tel:${STORE.phone}">${copy.phone}</a>
            </div>
          </div>
          <div class="map-frame">
            <iframe src="${STORE.mapEmbed}" loading="lazy" allowfullscreen referrerpolicy="no-referrer-when-downgrade" title="${copy.title} map"></iframe>
          </div>
        </div>
      </section>
      `}
    </main>

    <footer class="site-footer">
      <p>
        © ${new Date().getFullYear()} ${copy.footer}. All rights reserved. Powered by
        <a class="landing-footer__credit" href="https://reserveinjp.com/#/" target="_blank" rel="noopener noreferrer">reserveinjp</a>.
      </p>
    </footer>
  `;

  const menuButton = document.querySelector(".lang-menu-btn");
  const menu = document.getElementById("lang-menu");

  function closeMenu() {
    if (!menuButton || !menu) return;
    menu.hidden = true;
    menuButton.setAttribute("aria-expanded", "false");
  }

  function toggleMenu() {
    if (!menuButton || !menu) return;
    const nextOpen = menu.hidden;
    menu.hidden = !nextOpen;
    menuButton.setAttribute("aria-expanded", String(nextOpen));
  }

  if (menuButton && menu) {
    menuButton.addEventListener("click", (event) => {
      event.stopPropagation();
      toggleMenu();
    });

    menu.addEventListener("click", (event) => {
      event.stopPropagation();
    });

    document.addEventListener("click", closeMenu);
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") closeMenu();
    });
  }

  const carousel = document.querySelector("[data-carousel]");
  if (carousel) {
    const track = carousel.querySelector(".carousel-track");
    const slides = Array.from(carousel.querySelectorAll(".carousel-slide"));
    const prevBtn = carousel.querySelector("[data-carousel-prev]");
    const nextBtn = carousel.querySelector("[data-carousel-next]");
    const dots = Array.from(carousel.querySelectorAll("[data-carousel-dot]"));
    let index = 0;
    let timer = null;

    function setIndex(next) {
      if (!slides.length) return;
      index = (next + slides.length) % slides.length;
      track.style.transform = `translateX(${-index * 100}%)`;
      dots.forEach((dot, i) => {
        dot.classList.toggle("is-active", i === index);
        dot.setAttribute("aria-current", i === index ? "true" : "false");
      });
    }

    function start() {
      stop();
      timer = window.setInterval(() => setIndex(index + 1), 4500);
    }

    function stop() {
      if (timer) window.clearInterval(timer);
      timer = null;
    }

    prevBtn?.addEventListener("click", () => {
      stop();
      setIndex(index - 1);
      start();
    });

    nextBtn?.addEventListener("click", () => {
      stop();
      setIndex(index + 1);
      start();
    });

    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        stop();
        setIndex(Number(dot.dataset.carouselDot || 0));
        start();
      });
    });

    carousel.addEventListener("mouseenter", stop);
    carousel.addEventListener("mouseleave", start);
    carousel.addEventListener("focusin", stop);
    carousel.addEventListener("focusout", start);

    setIndex(0);
    start();
  }
}

render();
